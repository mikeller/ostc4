# Local Tools Workspace

This directory is intentionally ignored except for this guide. Use it for temporary scripts, local recovery experiments, and machine-specific tooling that should not become part of the repository interface.

Move a script to `../scripts/` only after it has stable behaviour, documented usage, and appropriate verification.
